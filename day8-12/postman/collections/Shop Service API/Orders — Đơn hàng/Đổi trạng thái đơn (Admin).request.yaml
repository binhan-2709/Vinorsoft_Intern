$kind: http-request
description: |-
  Chỉ admin mới đổi được trạng thái
  Các giá trị: pending, confirmed, shipping, delivered, cancelled
url: "{{base_url}}/api/v1/orders/{{order_id}}/update_status/"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "status": "confirmed"
    }
order: 5000
